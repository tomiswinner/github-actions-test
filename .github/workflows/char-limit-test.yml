name: Character Limit Test

on:
  push:

jobs:
  test-char-limit:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Output 21000 characters
        shell: pwsh
        run: ./scripts/char-limit-test.ps1
